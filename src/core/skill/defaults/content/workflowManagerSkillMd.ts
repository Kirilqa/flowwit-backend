export const WORKFLOW_MANAGER_SKILL_MD =
    '---\nname: workflow-manager\ndescription: Use when designing, creating, updating, running, or debugging WorkFlow entities in this system — deterministic node graphs, as opposed to agents. Covers the full node catalog, how to wire ports/connections/config correctly, the executionId mechanism behind loops and parallel branches, graph validation rules, the two different ways a workflow actually executes, and exactly which workflow_* tool to call for each action. Read this before building any workflow graph.\n---\n\n# Workflow Manager\n\nThis skill explains how the WorkFlow system works in this system and how to correctly build a node graph with the `workflow_*` tools. It is the most structurally involved of the entity-management skills — read it in full before attempting `workflow_create`, don\'t skim. For per-node-type exact schemas (ports/outputs/config), see the bundled `NODES.md` resource — load it once you know which node types you need.\n\n## What a WorkFlow is\n\nA WorkFlow is a **deterministic** graph of nodes — the opposite of an agent. It always executes the same steps in the same order for the same input; there is no reasoning, no tool-choice, no LLM deciding what happens next (individual nodes like `llm`/`agent` can call an LLM, but the graph structure itself never changes at runtime). Three objects matter:\n\n- **`WorkFlow`** — owns the graph definition: nodes (`entries`), `connections`, per-node `portMappings` and `configOverrides`. Pure description, never executes anything itself.\n- **`WorkFlowRun`** — one execution attempt. Holds a **snapshot** of the graph as it was at start time plus live per-node state (`executions`, keyed by `executionId`). Changing or deleting the workflow afterward does not affect runs already created from it.\n- **`WorkFlowRunner`** — walks the graph, moves data from node to node, manages parallel branches and loops via `executionId`.\n\nNodes themselves are dumb and stateless — one shared instance per node **type** is registered once in the system (`WorkFlowNodeRegistry`) and reused across every graph and every entry of that type. All actual execution state lives in the run\'s `executions`, never in the node object. This is why you can freely use the same node type (e.g. `transform`) many times in one graph, each with a distinct entry `id`.\n\n## Two different ways a workflow actually executes — decide this before designing\n\nThis matters for how you design the graph and what you should expect back:\n\n1. **As a tool for an agent** (`workflow__{workflowId}`, available once `workflow_register` has been called) — **synchronous and blocking**: the calling agent\'s tool call runs the entire graph to completion in-memory, then returns `run.getOutput()` directly as the tool result. This run is **never persisted** — it will not show up in `workflow_list_runs` or be inspectable via `workflow_run_info` afterward, win or fail. If it fails, the tool call itself throws. Argument shape: a single required `input` field (`{ "input": ... }`), passed through even if `null`.\n2. **Via the management tool `workflow_run`** — **fire-and-forget**: returns a `runId` immediately while the graph executes in the background, persisted to the run repository. Use `workflow_run_info`/`workflow_list_runs` to poll status and results, `workflow_stop` to abort mid-flight.\n\nDesign implication: a workflow meant to be a quick, synchronous capability for an agent (e.g. "format this text") should complete quickly and deterministically, since nothing about its progress is inspectable while path 1 is in flight. A long-running or exploratory workflow (batch processing, anything with an unbounded `while_loop`) should be run and monitored via path 2 instead.\n\n## Anatomy of a workflow definition\n\nA workflow is `entries[]` + `connections[]`:\n\n```typescript\nentries: [{\n    id: string                                    // unique within this workflow, you choose it\n    nodeType: string                              // e.g. "input", "transform" — from workflow_nodes\n    portMappings?: Record<string, InputMapping[]> // transform data as it arrives on a port\n    configOverrides?: Record<string, MappingValue> // fill this node\'s config, dynamically or statically\n}]\n\nconnections: [{\n    id?: string          // auto-generated if omitted\n    sourceNodeId: string\n    sourcePort: string    // an output port name on the source node\n    targetNodeId: string\n    targetPort: string    // an input port name on the target node\n}]\n```\n\n`InputMapping` = `{ targetParameter: string, value: MappingValue }`. `MappingValue` is either:\n\n- `{ type: "constant", data: string | number | boolean }` — a fixed value\n- `{ type: "expression", expression: string }` — a JS expression run in a sandboxed `vm` context (1s timeout) with **`$ports`** (all currently-received port values on this node, as an object) and **`$input`** (the specific incoming value that triggered this evaluation) both in scope\n\n`configOverrides` values are evaluated the same way, with `$ports` available (no single `$input`, since a config key isn\'t tied to one specific incoming port). Re-evaluated fresh every time the node executes — critical for loop conditions, see below.\n\nThere is a third `MappingValue` variant, `{ type: "function", fn }` — **code-first only**, never send this through the tools; it is stripped during serialization and the JSON Schema exposed to you never includes it.\n\n## The build pipeline\n\n1. **`workflow_nodes`** first, always — even if you think you remember the schema, config/port field names must match exactly or `workflow_create` rejects the graph. Do this before drafting `entries`.\n2. Design the graph on paper/in your head: which node is the entry point (must be `input` — the only `isStart: true` node type in this system), which nodes are terminal (no outgoing connection — their output becomes the workflow\'s result, keyed by node `id`), how data needs reshaping between incompatible ports (via `portMappings`/`configOverrides` expressions).\n3. **`workflow_create`** with the full `entries`/`connections`. It runs `WorkFlow.validate()` server-side before saving — see the exact rules below. A validation failure returns every violation, not just the first; fix them all before retrying.\n4. **`workflow_register`** so the workflow becomes callable as `workflow__{id}` — this only acts on the calling agent, there is no parameter to target a different one. To make it available to another agent, that agent has to call `workflow_register` itself.\n5. Test it — either call `workflow__{id}` directly (if registered) or `workflow_run` + `workflow_run_info` to inspect `nodeStates` per node/execution when something doesn\'t look right.\n6. To change the graph, **`workflow_update`** — providing `entries` or `connections` **replaces the entire respective list**, there is no incremental "add one node" tool. Re-send the full graph with your change folded in.\n\n## Validation rules — exactly what `workflow_create`/`workflow_update` check\n\nAll of these produce a rejection with a specific error message, not a partial success:\n\n- Every connection\'s `sourceNodeId`/`targetNodeId` must reference an entry that exists in `entries`.\n- A connection\'s `sourcePort` must be a real output port on the source node\'s `outputsJsonSchema`.\n- A connection\'s `targetPort` must be a real input port on the target node\'s `portsJsonSchema`.\n- **At least one start node is required** — at least one entry whose node type has `isStart: true` (only `input`) that has **no incoming connection** (an `input` node that does have an incoming connection is not treated as a valid start point).\n- Every `configOverrides` key must exist in that node\'s `configJsonSchema` properties.\n- Every `portMappings` key must be an **actually-incoming** port on that node — either a port that has a connection targeting it, or `$input` if the node is a start node. You cannot set a portMapping for a port nothing feeds.\n- Every `portMappings[...].targetParameter` must exist in that node\'s own `portsJsonSchema` — it\'s the field on the same node\'s ports that the mapping writes into.\n- (Enforced separately, at connection-add time rather than by `validate()`, but still fatal): a single `(targetNodeId, targetPort)` pair cannot receive two different connections — one incoming connection per input port, no fan-in merging at the wiring level. Use `merge` or `race` nodes if you need to combine multiple sources into one port.\n\n## Start nodes, final nodes, and the output shape\n\n- The **only** start node type is `input`. If a workflow needs multiple independent entry chains, use multiple `input` entries — but know that **every `input` node in the graph receives the identical `run.input` value** at the start of a run; there\'s no way to fan different starting values to different chains from one `input` argument. If you need per-chain differentiation, put a `transform`/`condition` node right after a single shared `input` node instead.\n- A **final node** is any entry with **no outgoing connection**. The workflow\'s overall output is an object keyed by each final node\'s own `id`, valued with that node\'s `output` from its **last completed execution**. A final node that never received any data (e.g. it sits on the `false` branch of a `condition` that resolved `true`) is simply absent/`undefined` in the result — not an error, not a null placeholder you can rely on being present.\n- Prefer meaningful final-node `id`s (e.g. `"summary"`, `"error"`) since they become the literal keys in the tool result an agent has to interpret.\n\n## The executionId mechanism — read this before building any loop or parallel branch\n\n`executionId` identifies one "thread" of data flowing through the graph. Every start node in a run is seeded with the same `initialExecutionId`. As data flows forward through ordinary connections, the executionId is carried along unchanged — this is what lets a `merge` node combine two ports that both trace back to the same origin.\n\nA node can spawn a **new branch** by returning `executionIds: { <outputPortName>: true }` alongside its output. Only the loop nodes (`for_loop`, `while_loop`) do this, and only for their `loop` output — never for `done`. When this happens:\n\n- The runner mints a brand-new `executionId` for whatever that `loop` output feeds into.\n- It\'s recorded that this new id\'s parent is the current id, and which node minted it.\n- Later, when data flowing under that child id is routed **back into the very node that minted it** (i.e. the loop-back connection closes the cycle into the loop node\'s own `loop` input), the runner swaps the executionId back to the parent. This is what keeps a loop node itself always executing under one stable id across iterations, while the loop **body** (the nodes between the `loop` output and the back-edge) run under a fresh id each pass.\n\n### Building a loop correctly — worked example\n\nDoubling a number 5 times: `input → for_loop → transform(×2) → back into for_loop\'s "loop" port`, exiting via `for_loop`\'s `done` into a final node.\n\n```jsonc\n{\n    "entries": [\n        { "id": "start", "nodeType": "input" },\n        {\n            "id": "loop",\n            "nodeType": "for_loop",\n            "configOverrides": { "iterations": { "type": "constant", "data": 5 } }\n        },\n        {\n            "id": "double",\n            "nodeType": "transform",\n            "configOverrides": { "expression": { "type": "constant", "data": "$input * 2" } }\n        },\n        { "id": "result", "nodeType": "json_stringify" }\n    ],\n    "connections": [\n        { "sourceNodeId": "start", "sourcePort": "result", "targetNodeId": "loop", "targetPort": "value" },\n        { "sourceNodeId": "loop", "sourcePort": "loop", "targetNodeId": "double", "targetPort": "value" },\n        { "sourceNodeId": "double", "sourcePort": "result", "targetNodeId": "loop", "targetPort": "loop" },\n        { "sourceNodeId": "loop", "sourcePort": "done", "targetNodeId": "result", "targetPort": "value" }\n    ]\n}\n```\n\nThe critical wire is the third connection: the loop body\'s last node (`double`) feeds **directly back into `loop`\'s own `loop` input port** — that specific shape (self-referencing back-edge into the loop node) is what the runner\'s parent/child executionId logic is built to recognize. Skipping it (e.g. routing the body\'s output somewhere else and never back to the loop node) means the loop node\'s `loop`/`value` port never receives a second value and the loop silently never iterates past once.\n\n`while_loop` is identical in wiring shape, except `configOverrides.condition` must be an `expression` that inspects `$ports.value`/`$ports.loop` and evaluates to a boolean each pass (unlike `for_loop`, there\'s no built-in iteration cap — an always-true expression loops forever until `workflow_stop` or process death).\n\n### `merge` and `race` need matching executionIds to behave as expected\n\n`merge` waits for both its `a` and `b` ports to have data **under the same executionId** — two branches that only ever shared a common ordinary-connection ancestor (no loop branching involved) satisfy this automatically. Two branches on different sides of a loop\'s branch-spawning boundary will not. `race` doesn\'t require matching ids at all — it just fires as soon as one of `a`/`b` arrives for a given pending execution; if `a` and `b` come from genuinely different executionIds, `race` fires **once per id**, not once globally. See `NODES.md` for the exact port shapes.\n\n## Security note: node references bypass per-agent grants\n\n`tool`, `mcp_tool`, `skill`, and `agent` nodes look their target up in the **system-wide registry** (`toolRegistry`/`mcpServerRegistry`/`skillRegistry`/`agentRegistry`) directly — not the calling agent\'s own `tools`/`mcpServers`/`skills`/`agents` lists. A workflow only needs `workflow_register` to become callable by an agent at all; once callable, its internal nodes can reach anything registered anywhere in the system, regardless of what that agent was individually granted. Be deliberate about what a workflow you hand to a low-privilege agent is allowed to embed.\n\n## The golden rule: never hand-edit workflow JSON files\n\nWorkflows are owned by `JsonWorkFlowRepository`/`WorkFlowRegistry` (definitions) and `JsonWorkFlowRunRepository` (run history). Always go through the `workflow_*` tools — hand-editing the underlying files skips `validate()` entirely, so a malformed graph fails silently at the next load instead of giving you the precise error list `workflow_create`/`workflow_update` give you immediately.\n\n## Tool reference\n\n| Tool                  | Purpose                                                                                                          | Key parameters                                                    |\n| --------------------- | ---------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |\n| `workflow_nodes`      | List every available node type with its ports/outputs/config JSON Schema. Call this first.                       | —                                                                 |\n| `workflow_create`     | Create a workflow from a full graph. Validates before saving.                                                    | `name`, `description?`, `entries`, `connections`                  |\n| `workflow_update`     | Replace an existing workflow\'s fields. `entries`/`connections` fully replace, not merge.                         | `workflowId`, `name?`, `description?`, `entries?`, `connections?` |\n| `workflow_delete`     | Permanently delete a workflow. Existing runs are unaffected.                                                     | `workflowId`                                                      |\n| `workflow_list`       | List all workflows with basic info (node/connection counts).                                                     | —                                                                 |\n| `workflow_info`       | Full serialized definition of one workflow (all entries, connections, mappings, overrides).                      | `workflowId`                                                      |\n| `workflow_register`   | Make an existing workflow callable as `workflow__{id}` for yourself.                                             | `workflowId`                                                      |\n| `workflow_unregister` | Remove a workflow from your own callable tools; the workflow itself stays in the system.                         | `workflowId`                                                      |\n| `workflow_run`        | Start a run in the background, persisted. Returns immediately with a `runId`.                                    | `workflowId`, `input?`                                            |\n| `workflow_run_info`   | Full snapshot of a run: status, output, and every node\'s per-execution state (input/config/output/error/timing). | `runId`                                                           |\n| `workflow_list_runs`  | List runs, optionally filtered by `workflowId` and/or `status`.                                                  | `workflowId?`, `status?`                                          |\n| `workflow_stop`       | Abort a running run. No-op if already finished.                                                                  | `runId`                                                           |\n\n## Naming convention\n\n`id` is generated server-side (`workflow_create` assigns a UUID, you don\'t choose it) and becomes the tool suffix `workflow__{id}` — no collision risk, unlike sub-agent tool names. Node entry `id`s within a graph are yours to choose and only need to be unique within that one workflow; prefer short, purpose-describing snake_case or kebab-case (`start`, `fetch-page`, `parse-json`) since they double as the keys in `workflow_run_info`\'s `nodeStates` and in the final output object.\n\n## Gotchas\n\n- `workflow_create`/`workflow_update` reject the **entire** graph on any validation error — there\'s no partial save. Read the full error list; multiple problems are reported together.\n- The `condition`/`while_loop` nodes\' `condition` config field is a **static boolean**, not a comparison you write against the port value directly — you must supply a `configOverrides.condition` expression yourself, or the branch/loop will always take the same path.\n- The tool-call execution path (`workflow__{id}`) leaves **no trace** in `workflow_list_runs`/`workflow_run_info` — if you need to debug a workflow an agent invoked as a tool, reproduce it via `workflow_run` instead so you get an inspectable run record.\n- `workflow_delete`/`workflow_update` don\'t touch any agent\'s `workflows` list synchronously — cleanup (removing a deleted workflow, or swapping in the fresh object after an update) happens on the next file-watcher tick via `WorkFlowUpdater`, same asynchronous pattern as skill/MCP/sub-agent cleanup. Immediately after deleting, an agent that had it registered may still be able to run it once more before the watcher catches up.\n- Only `constant` and `expression` mapping types are usable through these tools — never attempt to pass `{ type: "function", ... }`, it isn\'t representable in JSON and will fail schema validation on the tool call itself.\n- For the exact ports/outputs/config of every node type, load the bundled `NODES.md` resource rather than relying on memory — re-verify against a live `workflow_nodes` call if anything seems off, since that\'s the actual source of truth `validate()` uses.\n'
