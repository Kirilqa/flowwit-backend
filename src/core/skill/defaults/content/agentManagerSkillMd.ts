export const AGENT_MANAGER_SKILL_MD =
    "---\nname: agent-manager\ndescription: Use when creating, updating, deleting, or otherwise managing Agent entities in this system — including granting an agent tools/skills/sub-agents/MCP servers/workflows, setting guardrail permission rules scoped to an agent, or registering/unregistering an existing agent as your own sub-agent. Explains the Agent config model, the agent__ sub-agent delegation model, and exactly which agent_* tool to call for each action.\n---\n\n# Agent Manager\n\nThis skill explains how Agent entities work in this system and which tools to use to create, configure, and manage them. It does not teach you how to delegate a task to a sub-agent you already have — once registered, a sub-agent shows up as a regular `agent__{name}` tool in your own pool and is called directly.\n\n## What an Agent is\n\nAn agent is a persisted configuration plus a running instance built from it. On disk (`agents.json`, one array of `RawAgentConfig`):\n\n```typescript\n{\n    id: string                 // unique identifier, used everywhere as the reference key\n    name: string                // human-readable name — also becomes the sub-agent tool suffix, see Gotchas\n    role: 'assistant' | 'orchestrator' | 'specialist' | 'reviewer'  // descriptive only, see below\n    description?: string\n    provider: string             // exact provider name, must exist in the provider registry\n    model: string                // model identifier for that provider, not validated against a list\n    systemPrompt: string\n    thinkingStrategy: string      // exact strategy name, e.g. \"react\"\n    tools?: string[]              // glob patterns, see \"Glob patterns\" below\n    skills?: string[]             // glob patterns\n    agents?: string[]             // glob patterns, sub-agent IDs\n    mcpServers?: string[]         // glob patterns\n    workflows?: string[]          // glob patterns\n    budget?: { maxTokens?, maxIterations?, maxToolCalls?, maxCostUsd?, maxDurationMs? }\n    temperature?: number\n    metadata?: Record<string, unknown>\n    guardrailRules?: Record<string, Record<string, 'approve_always' | 'deny_always'>>  // guardrailId -> ruleKey -> decision, see \"Guardrail permission rules\"\n}\n```\n\n`role` is purely descriptive — it is injected as a line in the agent's own identity prompt (`## Identity` → `- Role: ...`) so the model knows how to characterize itself. It has no effect on execution, tool access, or behavior; pick whichever value best describes the agent's purpose for prompting quality, not for permissions.\n\n`budget`, `temperature` and `guardrailRules` have no glob/registry resolution for their _values_ — they are plain data copied through as-is. `guardrailRules` keys are validated though, see below.\n\n## Guardrail permission rules\n\nGuardrails (`tool_permission`, `shell_command`) normally ask a live human to approve or deny a sensitive action the first time it's attempted, then remember the decision. Besides the global rules (admin-managed, apply to every agent) and session rules (set by a user clicking \"always\" mid-conversation), an agent can carry its **own** permission rules that apply to it specifically, regardless of session. Priority when a decision is looked up: **global > this agent's rules > session**.\n\n`guardrailRules` shape: `{ [guardrailId]: { [ruleKey]: 'approve_always' | 'deny_always' } }`. The two guardrails currently in the system and what their `ruleKey` means:\n\n| `guardrailId`     | What it gates                                                                       | `ruleKey` meaning                                                      |\n| ----------------- | ----------------------------------------------------------------------------------- | ---------------------------------------------------------------------- |\n| `tool_permission` | Any tool call, except tools that never require confirmation (`done`, `human_input`) | Exact tool name, or a glob pattern (e.g. `\"fs_*\"`)                     |\n| `shell_command`   | Calls to `execute_command` specifically                                             | The first word of the shell command, or a glob pattern (e.g. `\"git*\"`) |\n\nRule keys support the same glob matching as tools/skills/etc. (via `picomatch`), with this resolution order when multiple rules could apply: an **exact** match always wins over a pattern match; if several rules of the same specificity (several exact, or several patterns) conflict, `deny_always` wins over `approve_always`.\n\n`agent_create`/`agent_update` validate `guardrailId` against the actual guardrail registry and throw `AgentToolError` on a typo. `ruleKey` is **not** validated — it's guardrail-specific and can be a pattern, so no generic check is possible; a typo'd `ruleKey` silently creates a rule that never matches anything, it will not error.\n\n## Glob patterns in tools/skills/agents/mcpServers/workflows\n\nThese five fields are not literal name lists — every entry is a glob pattern matched via `picomatch` against the relevant registry's current entries (tool names, skill names, sub-agent IDs, MCP server names, workflow IDs). A literal name like `\"search\"` is just a pattern that happens to match only itself, but you can also write:\n\n- `\"fs_*\"` — every tool starting with `fs_`\n- `\"*\"` — everything currently in that registry\n- multiple entries — each is matched independently and the results are unioned and de-duplicated\n\n`agent_create` takes a flat list per field (`tools`, `skills`, `agents`, `mcpServers`, `workflows`) — this is the agent's _initial_ state, there's nothing to conflict with yet. An unmatched pattern here is **silently skipped** with a server-side warning only — you will not see an error, you'll just get an agent with fewer tools than you expected. Always check the returned summary's lists against what you intended.\n\n`agent_update` does **not** take those flat fields at all. Instead each capability field is edited through an `add*`/`remove*` pair (`addTools`/`removeTools`, `addSkills`/`removeSkills`, `addAgents`/`removeAgents`, `addMcpServers`/`removeMcpServers`, `addWorkflows`/`removeWorkflows`):\n\n- `add*` — glob patterns to grant, resolved against the registry exactly like `agent_create`. An unmatched pattern **throws `AgentToolError` immediately** and nothing in the call is applied.\n- `remove*` — **not** glob patterns matched against current entities. Each string must match, character-for-character, a pattern string that is currently stored for this agent (i.e. exactly what was previously passed to `add*` or `agent_create`, e.g. `\"fs_*\"` — not `\"fs_read\"` even if that's one of the tools it currently grants, unless `\"fs_read\"` itself was the literal pattern originally added). A string that isn't currently present is **silently ignored**, not an error — safe to call defensively.\n- You can pass both `add*` and `remove*` for the same field in one call; additions are applied first, then removals — so a `remove*` entry always wins over an `add*` entry for the same pattern in that call.\n- Nothing you don't mention is touched. Untouched fields keep whatever patterns are already stored — you never need to read the current state first just to change one thing.\n\n`provider` and `thinkingStrategy` are the exception — these are single exact-match lookups against their registries, not glob patterns, on both tools. A typo there always throws.\n\n## The golden rule: never touch agents.json directly\n\nAgent configs are owned by `JsonRawAgentConfigRepository` and the in-memory `AgentRegistry`. Never hand-edit `agents.json` — always go through the `agent_*` tools below. Editing the file directly bypasses provider/strategy/tool/skill/mcp/workflow/guardrail resolution entirely, so a malformed entry silently fails to load at the next restart or file-watcher tick instead of giving you an immediate, actionable error.\n\n## Tool reference\n\n| Tool               | Purpose                                                                                                                                                                                                                                                                | Key parameters                                                                                                                                                                                                                                                                                                                                  |\n| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `agent_create`     | Create a new agent, persist it, and register it immediately. Fails if the ID already exists.                                                                                                                                                                           | `id`, `name`, `role`, `provider`, `model`, `systemPrompt`, `thinkingStrategy`, plus optional `description`, `tools`, `skills`, `agents`, `mcpServers`, `workflows`, `budget`, `temperature`, `metadata`, `guardrailRules`                                                                                                                       |\n| `agent_update`     | Patch an existing agent. Only provided fields change. Capability fields use `add*`/`remove*` pairs (see above), guardrail rules use `setGuardrailRules`/`removeGuardrailRules` (see below); everything else replaces the previous value entirely.                      | `agentId`, plus `name`, `role`, `description`, `provider`, `model`, `systemPrompt`, `thinkingStrategy`, `addTools`/`removeTools`, `addSkills`/`removeSkills`, `addAgents`/`removeAgents`, `addMcpServers`/`removeMcpServers`, `addWorkflows`/`removeWorkflows`, `budget`, `temperature`, `metadata`, `setGuardrailRules`/`removeGuardrailRules` |\n| `agent_delete`     | Remove the config and unregister the agent. Cannot target yourself.                                                                                                                                                                                                    | `agentId`                                                                                                                                                                                                                                                                                                                                       |\n| `agent_list`       | List every agent in the system with a config summary, including current `budget`/`temperature`/`guardrailRules`, resolved entity names, and the raw glob patterns behind them (`toolPatterns`/`skillPatterns`/`agentPatterns`/`mcpServerPatterns`/`workflowPatterns`). | —                                                                                                                                                                                                                                                                                                                                               |\n| `agent_info`       | Get one agent's full config summary — same fields as `agent_list`, for a single agent.                                                                                                                                                                                 | `agentId`                                                                                                                                                                                                                                                                                                                                       |\n| `agent_register`   | Register an existing agent as **your own** sub-agent, callable as `agent__{name}`. Cannot target yourself.                                                                                                                                                             | `agentId`                                                                                                                                                                                                                                                                                                                                       |\n| `agent_unregister` | Remove a sub-agent from yourself. The agent itself keeps existing in the system.                                                                                                                                                                                       | `agentId`                                                                                                                                                                                                                                                                                                                                       |\n\n## Step-by-step scenarios\n\n**Create a new agent from scratch:**\n\n1. Make sure the `provider` (e.g. `openai`, `openrouter`) and `thinkingStrategy` (e.g. `react`) you want already exist — `agent_create` fails immediately if either is missing.\n2. `agent_create` with `id`, `name`, `role`, `provider`, `model`, `systemPrompt`, `thinkingStrategy`, and any of `tools`/`skills`/`agents`/`mcpServers`/`workflows` as glob patterns to grant capabilities right away, plus `guardrailRules` if you already know it needs standing permission rules.\n3. Check the returned summary's lists match what you intended — a mistyped pattern is not an error here, just an empty match.\n\n**Grant or change an agent's capabilities after creation:**\n\n1. `agent_update` with `agentId` and the specific `add*`/`remove*` pairs for what's changing — e.g. `addTools: [\"fs_*\"]` to grant, `removeTools: [\"fs_*\"]` to revoke exactly that pattern. No need to read the current state first just to grant something new; anything you don't mention is left alone.\n2. If you need to revoke something but don't remember the exact pattern that granted it, call `agent_info` first and read `toolPatterns`/`skillPatterns`/`agentPatterns`/`mcpServerPatterns`/`workflowPatterns` — these carry the raw pattern strings, not the resolved names the plain `tools`/`skills`/... fields show.\n3. A bad pattern in `add*` throws immediately and nothing is changed; a `remove*` pattern that isn't currently present is just ignored, not an error.\n4. Check `agent_info` afterwards if you want to confirm the resulting state.\n\n**Set or change an agent's standing guardrail permissions:**\n\n1. `agent_update` with `setGuardrailRules: [{ guardrailId, ruleKey, decision }]` to create or overwrite specific rules — see \"Guardrail permission rules\" above for what `guardrailId`/`ruleKey` mean.\n2. `removeGuardrailRules: [{ guardrailId, ruleKey }]` to remove specific rules — silently ignored if that exact pair isn't currently set.\n3. `agent_info` shows the agent's current `guardrailRules` if you need to check before changing something.\n\n**Delete an agent:**\n\n- `agent_delete` — permanent, cannot be undone, and an agent cannot delete itself (`agentId` equal to the caller's own ID is rejected). Other agents that had it registered as a sub-agent get that reference removed automatically — a file-watcher-driven `AgentConfigUpdater` sweeps every agent's `agents` list shortly after the delete, same asynchronous pattern as skill/MCP cleanup (not instantaneous within the `agent_delete` call itself).\n\n**Give yourself access to an existing agent as a sub-agent:**\n\n1. `agent_register` with the target `agentId` — the agent must already exist in the system (create it first with `agent_create` if not). You cannot register yourself.\n2. It's now callable as `agent__{name}` (note: by `name`, not `id` — see Gotchas) with a `task` (required) and optional `outputSchema` (JSON Schema) argument.\n3. `agent_unregister` to remove it later; the target agent itself is untouched.\n\n**Grant an agent to someone other than yourself:**\n\n- Same limitation as skills/MCP/workflows: `agent_register`/`agent_unregister` always act on the calling agent, there is no parameter for a different target. Either have that agent call `agent_register` itself, or use `agent_update` on it with `addAgents`/`removeAgents`.\n\n## How sub-agent delegation actually works\n\nWhen you call `agent__{name}`, the system creates a **brand-new, isolated session** for the sub-agent, runs it to completion on your `task` string alone, captures its final message (or `structuredOutput` if you passed `outputSchema`), then deletes that session. The sub-agent has zero visibility into your conversation history — the `task` argument must be fully self-contained (the tool's own description reminds you of this). This is a one-shot call, not a persistent conversation: calling the same `agent__{name}` tool twice starts two unrelated sessions.\n\n## Naming convention\n\n`id` is the registry key and must be unique — enforced by `agent_create`. `name` has no uniqueness enforcement anywhere in the system, but it silently determines the sub-agent tool name (`agent__{name}`) — see the next section for why that matters. Prefer kebab-case or snake_case `id`s consistent with the rest of this system's naming (e.g. `code-reviewer`, `pdf_extractor`); `name` can be more human-readable prose since it's just a label plus a tool suffix.\n\n## Gotchas\n\n- **Sub-agent tool names are derived from `name`, not `id`.** If a parent agent registers two sub-agents that happen to share the same `name`, both get wrapped as `agent__{name}` and the second one silently overwrites the first in the tool pool — the first becomes unreachable, with no error anywhere. When creating an agent you intend to use as a sub-agent, give it a `name` that's unique at least among agents likely to be registered together.\n- **No cycle detection.** If agent A has agent B registered as a sub-agent and B has A registered back, nothing stops a task from bouncing between them indefinitely (each hop is a fresh isolated session, so this is real recursion, not shared state). Be deliberate about sub-agent graphs you build.\n- **`removeTools`/`removeSkills`/etc. match the stored pattern string exactly, not as a new glob.** `removeTools: [\"fs_*\"]` only removes an entry if `\"fs_*\"` itself was granted — it does not re-interpret `\"fs_*\"` as a glob and remove every tool whose name currently matches it. Plain `tools`/`skills`/`agents`/`mcpServers`/`workflows` on `agent_info`/`agent_list` show already-resolved exact names (e.g. `\"fs_read\"`, `\"fs_write\"`), not what to pass to `remove*` — use the sibling `toolPatterns`/`skillPatterns`/`agentPatterns`/`mcpServerPatterns`/`workflowPatterns` fields instead, which carry the raw pattern strings exactly as they are persisted.\n- **`agent_delete`/self-management guards are ID-based, checked against the calling agent's own ID** (from tool-call context) — an agent cannot delete or register itself, but there is nothing stopping agent A from deleting agent B even if B is currently mid-execution. That in-flight run is not aborted by the delete; only future access to `agent__B` (through anyone's sub-agent list) goes away.\n- A budget (`maxTokens`/`maxIterations`/`maxToolCalls`/`maxCostUsd`/`maxDurationMs`) is entirely optional — omitting it means no limit is enforced for that agent.\n- **`guardrailRules` on an agent affects only that agent's own runs.** It has no effect on sessions where a different agent handles the message (e.g. via the `/agent` command delegating a single turn) — the rules are keyed to whichever agent is actually executing, not to the session.\n"
