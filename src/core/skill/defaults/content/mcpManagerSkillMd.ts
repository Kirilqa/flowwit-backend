export const MCP_MANAGER_SKILL_MD =
    "---\nname: mcp-manager\ndescription: Use when adding, updating, removing, or otherwise managing MCP (Model Context Protocol) server connections in this system — including checking a server's real connection status, or registering/unregistering an MCP server for an agent so its tools/resources/prompts become callable. Explains the MCP server config model and exactly which mcp_* tool to call for each action.\n---\n\n# MCP Manager\n\nThis skill explains how MCP server connections work in this system and which tools to use to add, configure, and manage them. It does not teach you how to call a specific MCP server's tools — once registered, those show up as regular tools in your own pool and are called directly, not through this skill.\n\n## What an MCP server is here\n\nAn MCP server is an external process or endpoint speaking the Model Context Protocol, which this system connects to as a client and exposes to agents as tools. Configuration is a discriminated union on `type`:\n\n```typescript\n// local process, communicates over stdio\n{ name: string, type: 'stdio', command: string, args?: string[], env?: Record<string, string> }\n\n// remote endpoint\n{ name: string, type: 'streamable-http' | 'sse', url: string, headers?: Record<string, string> }\n```\n\n`name` is the unique identifier (also called `alias` once turned into a running client) — no separate slug/id. Configs are persisted as a JSON collection (owned by `JsonMCPServerConfigRepository`, one entry per server), completely separate from `agents.json` — an MCP server config existing in the system says nothing about which agents can use it.\n\n## Connection lifecycle — read this before trusting a status field\n\nAdding or updating a server does **not** connect synchronously. Under the hood: the tool creates an `MCPClient` and calls `mcpServerRegistry.register()`, which hands the client to a connection manager that starts connecting in the background and keeps retrying with exponential backoff (up to 10 attempts, capped at 30s between attempts) if the connection drops or fails. This means:\n\n- Immediately after `mcp_add` or `mcp_update`, the returned `status` will most often be `connecting`, not `connected` — this is normal, not a failure.\n- A server can independently be in `disconnected`, `connecting`, `connected`, or `error`. Always call `mcp_info` after a short pause if you need to confirm a server actually came up before telling the user it's ready.\n- A server sitting in `error` keeps retrying on its own in the background — there is no explicit \"retry now\" tool, but calling `mcp_update` with just `{ name }` and no other changes forces the client to be rebuilt and reconnected immediately, which is a practical way to force a retry without waiting for backoff.\n- Registering a server to an agent (`mcp_register`) can succeed even while the server is still `connecting` or in `error` — registration only attaches the server to the agent's config, it does not require a live connection. The server's tools will simply be silently absent from that agent's tool pool until the connection reaches `connected` at the moment the pool is next built (each agent run rebuilds its tool pool from scratch). If expected MCP tools don't show up, check `mcp_info` before assuming `mcp_register` failed.\n\n## How an MCP server's capabilities become tools\n\nA connected server can expose tools, resources, and/or prompts (check via `mcp_info` → the underlying client's capabilities: `hasTools`/`hasResources`/`hasPrompts`). When an agent that has this server registered builds its tool pool, each capability is namespaced and sanitized (non-alphanumeric characters become `_`):\n\n- Tools → `{alias}__{toolName}`\n- Resources → `{alias}__resource__{resourceName}`\n- Prompts → `{alias}__prompt__{promptName}`\n\nOnly servers with status `connected` at pool-build time contribute any of these — a disconnected or erroring server contributes nothing, silently (no error tool, no placeholder).\n\n## The golden rule: never touch mcp.json directly\n\nThe server config collection is owned by `JsonMCPServerConfigRepository` and the in-memory `MCPServerRegistry`. Never hand-edit the underlying JSON file — always go through the `mcp_*` tools below. Editing the file directly skips the connection manager entirely, so nothing will actually connect until a restart, and the in-memory registry silently drifts from what's on disk.\n\n## Tool reference\n\n| Tool             | Purpose                                                                                                        | Key parameters                                                                                                   |\n| ---------------- | -------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |\n| `mcp_add`        | Register a new server config, persist it, and start connecting. Fails if the name already exists.              | `name`, `server: { type: 'stdio', command, args?, env? }` or `{ type: 'streamable-http'\\|'sse', url, headers? }` |\n| `mcp_update`     | Patch an existing server's config. Only provided fields change; the client is rebuilt and reconnected.         | `name`, plus any of `command`/`args`/`env` (stdio) or `url`/`headers` (http/sse)                                 |\n| `mcp_delete`     | Remove the config from disk and disconnect the client. Agents that had it registered lose access to its tools. | `name`                                                                                                           |\n| `mcp_list`       | List every server in the system with its config and current connection status.                                 | —                                                                                                                |\n| `mcp_info`       | Get one server's config and current connection status.                                                         | `name`                                                                                                           |\n| `mcp_register`   | Attach an existing server to **yourself**, making its capabilities callable once connected.                    | `serverName`                                                                                                     |\n| `mcp_unregister` | Detach a server from yourself. The server keeps running system-wide.                                           | `serverName`                                                                                                     |\n\n**Critical distinction:** `mcp_list`/`mcp_info`/`mcp_add`/`mcp_update` return a `status` field reflecting the real MCP connection state (`disconnected`/`connecting`/`connected`/`error`) — treat `connecting` and `error` as \"not usable yet\", not as errors in the tool call itself.\n\n## Step-by-step scenarios\n\n**Add a local (stdio) MCP server:**\n\n1. `mcp_add` with `name` and `server: { type: 'stdio', command, args?, env? }` — `command` is executed as a local OS process.\n2. `mcp_info` shortly after to confirm it reached `connected` (it starts as `connecting`).\n3. `mcp_register` (called by the agent that should use it) to attach it to that agent's tool pool.\n\n**Add a remote (HTTP/SSE) MCP server:**\n\n1. `mcp_add` with `name` and `server: { type: 'streamable-http', url, headers? }` (or `type: 'sse'` for SSE transport).\n2. Same follow-up as above: `mcp_info` to confirm `connected`, then `mcp_register`.\n\n**Update an existing server's connection details:**\n\n1. `mcp_update` with `name` and only the fields that change (e.g. a new `url`, rotated `headers`, or a different `command`). This always creates a fresh client and reconnects, even if you also pass fields unchanged.\n2. No manual re-registration needed, but propagation to already-registered agents is asynchronous: `mcp_update` immediately updates the system-wide registry, and a file-watcher-driven `MCPConfigUpdater` then replaces the server reference inside every agent's `mcpServers` that already has it. Wait for `connected` via `mcp_info` before relying on the new config.\n\n**Remove a server entirely:**\n\n- `mcp_delete` — deletes the config and disconnects. The same file-watcher-driven `MCPConfigUpdater` also removes the server from every agent's `mcpServers` list shortly after (comparable to how `SkillsUpdater` cleans up skills on delete) — so this is not instantaneous within the `mcp_delete` call itself, but you don't need a separate cleanup step.\n\n**Make a server available to another agent (not yourself):**\n\n- `mcp_register`/`mcp_unregister` always act on the calling agent — there is no parameter to target a different agent. To grant a server to a different agent, that agent has to call `mcp_register` itself.\n\n## Naming convention\n\nServer `name` doubles as the tool-namespace prefix (`{alias}__...`), and this project's tools are snake_case/lowercase — use lowercase names with hyphens or underscores (e.g. `github-mcp`, `local_fs`), not spaces or PascalCase, so the generated tool names stay consistent with the rest of the pool.\n\n## Gotchas\n\n- Adding an stdio server runs an arbitrary local command with the environment variables you provide — there is no shell-command-specific guardrail on `mcp_add` (that guardrail only watches the dedicated `execute_command` tool). Only the generic tool-permission guardrail applies to the `mcp_add` call itself. Be as deliberate about `command`/`env` here as you would with directly executing a command.\n- A `connected: true`-style boolean is gone from this API on purpose — always reason in terms of the `status` enum, and don't assume `connecting` will resolve to `connected` immediately.\n- `mcp_delete`/`mcp_update` only touch the system-wide config and registry synchronously; cleanup of already-registered agents' `mcpServers` lists happens on the next file-watcher tick via `MCPConfigUpdater`, not within the tool call itself.\n- Capability names collide across servers only if you reuse the same `name` — the registry key is `name`, so `mcp_add` correctly rejects duplicates up front.\n"
