export const WORKFLOW_MANAGER_NODES_MD =
    "# Node Reference\n\nExact ports/outputs/config for every node type registered in this system. Field types are the Zod schema shapes; `?` means optional. Always cross-check against a live `workflow_nodes` call — this file can drift if new node types are added, but the underlying JSON Schema returned by that tool is the ground truth for the exact `properties` keys `WorkFlow.validate()` checks against.\n\n## Start node\n\n### `input` — the only start node in the system\n\n- **Ports:** none — receives `$input` implicitly (every workflow run seeds every `input` node's `$input` with the run's `input` argument, unaffected by portMappings targeting `$input` unless you override via `configOverrides`/`portMappings` — see SKILL.md)\n- **Outputs:** `result: unknown` — literally the input value, unchanged\n- **Config:** none\n- If a workflow has multiple `input` nodes, **all of them receive the exact same `run.input` value** — there is no way to give two start nodes different starting values from a single run.\n\n## Flow control nodes\n\n### `condition`\n\n- **Ports:** `value: unknown`\n- **Outputs:** `true: unknown?`, `false: unknown?` — only one fires per execution, carrying `value` through\n- **Config:** `condition: boolean` — a **plain boolean**, not an expression evaluated against `value`. To branch on the actual port data, set a `configOverrides.condition` of type `expression` referencing `$ports.value` (or `$input` if this node is itself a start-adjacent target), e.g. `\"$ports.value > 5\"`.\n\n### `merge`\n\n- **Ports:** `a: unknown`, `b: unknown` — both **required**, this node only runs once both have arrived\n- **Outputs:** `result: { a: unknown, b: unknown }`\n- **Config:** none\n- Both inputs must carry the **same `executionId`** to land in the same pending execution — see \"executionId\" in SKILL.md. Two branches that diverged through a loop's new-branch mechanism will never merge here.\n\n### `race`\n\n- **Ports:** `a: unknown?`, `b: unknown?` — fires as soon as **either** arrives (custom `isReady`)\n- **Outputs:** `result: unknown` — whichever of `a`/`b` arrived (`a ?? b`)\n- **Config:** none\n- If `a` and `b` arrive under different `executionId`s, this node runs **once per distinct executionId**, not once total — it is not a global race across the whole run, only within a single execution's pending state.\n\n### `for_loop`\n\n- **Ports:** `value: unknown?` (first entry), `loop: unknown?` (back-edge entry) — fires when either arrives\n- **Outputs:** `loop: unknown?` (continue), `done: unknown?` (exit)\n- **Config:** `iterations: number` (integer, min 1) — fixed iteration count\n- **State:** `iteration: number` (default 0), persisted across iterations automatically by the runner\n- Emitting on `loop` sets `executionIds: { loop: true }`, spawning a fresh child executionId for that branch every iteration. See SKILL.md's loop-building walkthrough — the `loop` output must connect back to this same node's `loop` input to close the cycle.\n\n### `while_loop`\n\n- **Ports:** `value: unknown?`, `loop: unknown?` — same start semantics as `for_loop`\n- **Outputs:** `loop: unknown?`, `done: unknown?`\n- **Config:** `condition: boolean` — same caveat as the `condition` node: this is a static boolean per execution, drive it with a `configOverrides.condition` expression reading `$ports.value`/`$ports.loop` to implement an actual loop-continuation test. Re-evaluated fresh on every iteration since `configOverrides` are recomputed from current `$ports` each time the node executes.\n- No built-in max-iteration safety — an always-true expression loops forever until the run is stopped (`workflow_stop`) or the process is killed.\n\n## Data transformation nodes\n\n### `transform`\n\n- **Ports:** `value: unknown`\n- **Outputs:** `result: unknown`\n- **Config:** `expression: string` — a JS expression evaluated in a sandboxed Node `vm` context (1 second timeout) with `$input` bound to `value`. Runtime errors here abort the run with a node failure.\n\n### `json_parse`\n\n- **Ports:** `value: string`\n- **Outputs:** `result: unknown`\n- **Config:** none — throws a node error on invalid JSON.\n\n### `json_stringify`\n\n- **Ports:** `value: unknown`\n- **Outputs:** `result: string`\n- **Config:** `indent: number` (0–8, default 0)\n\n### `delay`\n\n- **Ports:** `value: unknown`\n- **Outputs:** `result: unknown` — passthrough after waiting\n- **Config:** `delayMs: number` (default 1000)\n\n## Integration nodes\n\n### `http_request`\n\n- **Ports:** `trigger: unknown` — value is ignored, only its arrival matters (fires the request)\n- **Outputs:** `result: unknown` (parsed JSON if `content-type` is `application/json`, else raw text), `status: number`, `headers: Record<string, string>`\n- **Config:** `url: string` (must be a valid URL), `method: GET\\|POST\\|PUT\\|PATCH\\|DELETE\\|HEAD\\|OPTIONS`, `bodyFormat: json\\|text\\|form` (default `json`), `headers: Record<string,string>` (default `{}`), `body?: unknown`, `timeoutMs: number` (default 30000)\n- `body` is only sent if provided; `content-type` header is auto-set from `bodyFormat` when you haven't set one yourself.\n\n### `llm`\n\n- **Ports:** `prompt: string`\n- **Outputs:** `text: string`\n- **Config:** `providerName: string` (must exist in the provider registry, e.g. `openai`/`openrouter`), `model: string`, `systemPrompt?: string`, `messages?: unknown[]` (raw provider message objects, prepended before the port's `prompt`), `temperature?: number` (0–2), `maxTokens?: number`\n- Streams text deltas as `NODE_EVENT` payloads (`{ delta: string }`) while running — useful if you're consuming the run's live event stream (via `workflow_run` + `workflow_run_info` polling), not observable from the synchronous `workflow__{id}` tool call path.\n- This is a **direct, single-turn provider call** — no tools, no agent loop. Use the `agent` node if you need an agent (tool use, multi-turn reasoning).\n\n### `tool`\n\n- **Ports:** `args: Record<string, unknown>`\n- **Outputs:** `result: unknown`\n- **Config:** `toolName: string` — looked up in the **system-wide tool registry**, not the calling agent's own granted tool list. See SKILL.md's security note.\n\n### `mcp_tool`\n\n- **Ports:** `args: Record<string, unknown>`\n- **Outputs:** `result: unknown`\n- **Config:** `serverAlias: string`, `toolName: string` — throws if the server isn't registered system-wide or isn't currently in the `connected` status (a server can also be `disconnected`, `connecting`, or `error`).\n\n### `skill`\n\n- **Ports:** `trigger: unknown` — value ignored, only arrival matters\n- **Outputs:** `result: string` — the full rendered skill content (same text a `skill__{name}` tool call would return), not just the skill's raw markdown\n- **Config:** `skillName: string` — system-wide skill registry lookup, independent of any agent's granted skills.\n\n### `agent`\n\n- **Ports:** `prompt: string`\n- **Outputs:** `result: { message: string, messages: unknown[], usage: unknown }`\n- **Config:** `agentId: string` (system-wide agent registry lookup — not scoped to the calling agent's sub-agents), `systemPrompt?: string`, `messages?: unknown[]` (seeded into the sub-run's session before `prompt`), `outputSchema?: Record<string, unknown>` (JSON Schema — if set, `agent.run` extracts structured output, but this node always returns the same `result` shape; check `message` for the extractor's rendering)\n- Runs the target agent to completion in a **fresh, isolated session** created and destroyed for this node execution only — same isolation model as `agent__{name}` sub-agent delegation.\n- Streams every one of the sub-agent's own events (thinking, tool calls, etc.) as `NODE_EVENT` payloads.\n"
