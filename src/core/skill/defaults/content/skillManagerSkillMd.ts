export const SKILL_MANAGER_SKILL_MD =
    "---\nname: skill-manager\ndescription: Use when creating, updating, deleting, reading, or otherwise managing Skill entities in this system — including adding resource files to a skill, registering or unregistering a skill for an agent, or installing/updating skills from the ClawHub marketplace. Explains the Skill data model and exactly which skill_*/clawhub_* tool to call for each action.\n---\n\n# Skill Manager\n\nThis skill explains how Skills work in this system and which tools to use to create, edit, and manage them. It does not teach you how to use a skill's content — it teaches you how to build and maintain the skills themselves.\n\n## What a Skill is\n\nA Skill is a package of instructions plus optional supporting files that a specific agent can call as a tool. On disk it is a directory:\n\n```\nskills/{name}/SKILL.md\nskills/{name}/<any resource files, nested freely>\n```\n\n`SKILL.md` has YAML frontmatter followed by markdown instructions:\n\n```markdown\n---\nname: my-skill\ndescription: One or two sentences describing what this skill does and when to use it.\nlicense: MIT\ncompatibility: Node.js >= 18\nallowed-tools: fs_read fs_write\nmetadata:\n    category: data\n---\n\nFull instructions for the agent go here, in markdown.\n```\n\n- `name` and `description` are required. Everything else is optional.\n- `allowed-tools` (comma/space separated in frontmatter, array in the tool APIs) is stored as metadata only — it is **not enforced** by the runtime today. Treat it as documentation for whoever reads the skill, not as an access-control mechanism.\n- `metadata` is a free-form key-value bag, not interpreted by the system.\n- Resource files live anywhere under the skill directory except `SKILL.md` itself. The repository auto-discovers them recursively.\n\n## Three kinds of bundled content\n\nA skill's files fall into three categories, distinguished purely by where you put them — there is no type field, the path is the signal:\n\n- **Instructions** — extra markdown files (e.g. `FORMS.md`, `references/schema.md`) that `SKILL.md` points to for detail it doesn't want to carry inline. Read on demand with `skill_resource_read`.\n- **Code** — executable scripts, conventionally under `scripts/` (e.g. `scripts/fill_form.py`). These are meant to be **run**, not read — see `skill_resource_run` below. Running a script keeps its source out of context entirely; only its output counts against your budget.\n- **Resources/assets** — templates, images, fonts, and other binary or reference material, conventionally under `assets/`. These are not readable as text (see the Gotchas section) — a script that ships alongside them opens and manipulates them directly on disk.\n\nUsing the `scripts/` / `references/` / `assets/` folder convention is not enforced for anything except execution (`skill_resource_run` refuses to run anything outside `scripts/`), but following it keeps a skill's intent obvious and matches the wider Agent Skills ecosystem.\n\n## How a Skill is exposed to an agent — progressive disclosure\n\nA Skill is never given to an agent as raw text up front. It is wrapped in a tool named `skill__{name}` with:\n\n- `description` = the skill's `description` field — this is the **only** thing that sits in the agent's system prompt at all times (cheap, level 1).\n- No parameters — the tool takes `{}`.\n\nWhen the agent decides the skill is relevant and calls `skill__{name}`, the full `content` of `SKILL.md` is returned (level 2), wrapped as `[Skill: name] ... <instructions>...</instructions>`, followed by a `<resources>` block listing bundled resource paths. The agent then loads individual resources on demand (level 3) through `skill_resource_read` for text references, or `skill_resource_run` for anything under `scripts/` — never with raw filesystem tools (see the golden rule below). Resource content is never dumped into context automatically.\n\nPractical implications:\n\n- Keep `description` short but precise — it is the only signal deciding whether the skill gets invoked at all.\n- Keep `content` focused on what's needed for most invocations; push large reference material (long tables, exhaustive examples) into resource files instead of inflating `content`.\n- A skill only becomes callable for an agent that has it in `config.skills`. Being present in the system-wide skill registry is not enough by itself — see the registration scenarios below.\n\n## The golden rule: never touch skill files directly\n\n`./skills/**` is owned by `MarkdownSkillRepository` and the in-memory `SkillRegistry`. Never create, edit, or delete files under `./skills/` with filesystem tools (`fs_write`, `fs_delete`, etc.) — always go through the `skill_*` tools below. Reasons:\n\n- The repository is what could change tomorrow (e.g. move from files to a database) without any of these tools changing their contract.\n- Writing files directly bypasses the in-memory registry — the running system won't see your change until a restart or a file-watcher tick, and any agent relying on the skill will silently keep using stale content.\n- `skill_create`/`skill_update` serialize the frontmatter correctly (YAML escaping, field ordering); hand-written frontmatter is a common source of parse errors.\n\n## Tool reference\n\n| Tool                     | Purpose                                                                               | Key parameters                                                                               |\n| ------------------------ | ------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |\n| `skill_create`           | Create a brand-new skill. Saves to disk and registers it system-wide.                 | `name`, `description`, `content`, `license?`, `compatibility?`, `allowedTools?`, `metadata?` |\n| `skill_update`           | Patch an existing skill. Only provided fields change.                                 | `name`, plus any of the fields above, all optional                                           |\n| `skill_delete`           | Remove `SKILL.md` and unregister the skill. Directory and resources are left on disk. | `name`                                                                                       |\n| `skill_list`             | List all skills known to the system (management view — not \"what can I use\").         | —                                                                                            |\n| `skill_read`             | Read a skill's full content, for the purpose of editing it.                           | `name`                                                                                       |\n| `skill_resource_write`   | Create or overwrite a resource file inside a skill's directory.                       | `skillName`, `relativePath`, `content`                                                       |\n| `skill_resource_read`    | Read a resource file's content as text.                                               | `skillName`, `relativePath`                                                                  |\n| `skill_resource_delete`  | Delete a resource file (not `SKILL.md`).                                              | `skillName`, `relativePath`                                                                  |\n| `skill_resource_list`    | List all resource files of a skill (excludes `SKILL.md`).                             | `skillName`                                                                                  |\n| `skill_resource_run`     | Execute a script bundled under `scripts/` and return its stdout/stderr/exit code.     | `skillName`, `relativePath`, `args?`, `timeoutMs?`                                           |\n| `skill_register`         | Attach an existing system skill to **yourself**, making `skill__{name}` callable.     | `skillName`                                                                                  |\n| `skill_unregister`       | Detach a skill from yourself. Skill remains installed system-wide.                    | `skillName`                                                                                  |\n| `clawhub_search`         | Search the ClawHub marketplace for skills to install.                                 | `query`, `limit?`                                                                            |\n| `clawhub_skill_info`     | Get full marketplace details + moderation status for a slug.                          | `slug`                                                                                       |\n| `clawhub_skill_versions` | List published versions of a marketplace skill.                                       | `slug`, `limit?`                                                                             |\n| `clawhub_install`        | Download, safety-check, and install a marketplace skill. Fails if already installed.  | `slug`, `version?`                                                                           |\n| `clawhub_update`         | Update an already-installed marketplace skill to a newer version.                     | `slug`, `version?`                                                                           |\n\n**Critical distinction:** `skill_list` and `skill_read` exist only for management (browsing/editing skills). They are explicitly **not** how you use a skill's instructions in your own work — to actually use a skill you have registered, call its `skill__{name}` tool directly. Calling `skill_read` to \"consult\" a skill instead of invoking `skill__{name}` defeats the progressive-disclosure design and is the wrong tool for that job.\n\n## Step-by-step scenarios\n\n**Create a new skill from scratch:**\n\n1. `skill_create` with `name`, `description`, `content` (and any of `license`/`compatibility`/`metadata`).\n2. It is now installed system-wide, but not usable by anyone yet.\n3. `skill_register` (called by the agent that should use it) to attach it to that agent's tool pool.\n\n**Update an existing skill's instructions:**\n\n1. Optionally `skill_read` first if you need to see the current content before patching it.\n2. `skill_update` with `name` and only the fields you want to change — omitted fields are left untouched.\n3. No re-registration needed. `skill_update` writes the new `SKILL.md` to disk and updates the system-wide registry immediately. A file-watcher-driven `SkillsUpdater` then propagates the updated skill object to every agent that already has it registered, so their `skill__{name}` tool serves the new content shortly after — this is asynchronous (tied to the file watcher), not instantaneous within the same tool call.\n\n**Add supporting files to a skill (scripts, templates, reference docs):**\n\n1. `skill_resource_write` with `skillName`, a `relativePath` (nested paths allowed, e.g. `examples/usage.ts`), and `content`. Put executable scripts under `scripts/` — that path is what makes them runnable later.\n2. Reference the file by name in the skill's `content` so the agent knows to look for it — the `<resources>` block only lists paths, it doesn't explain what each file is for.\n3. `skill_resource_list` / `skill_resource_read` to inspect what's there; `skill_resource_delete` to remove one (this never touches `SKILL.md`).\n\n**Run a script bundled with a skill:**\n\n1. `skill_resource_run` with `skillName` and the script's `relativePath` — it must be under `scripts/`, or the call is rejected.\n2. The interpreter is picked automatically from the file extension (`.py` → `python3`, `.js` → `node`, `.sh` → `sh`); anything else is rejected.\n3. Pass extra CLI arguments via `args`, and raise `timeoutMs` above the 2-minute default for a script you know runs long — don't guess low and retry, that just wastes a call.\n4. Only stdout/stderr/exit code come back to you — the script's own source never enters your context, so this is the efficient way to do deterministic work a skill bundles for you, instead of reading the script and reimplementing its logic yourself.\n\n**Delete a skill entirely:**\n\n1. `skill_delete` removes `SKILL.md` and unregisters it — the skill stops being creatable/registrable.\n2. Leftover resource files and the empty directory are not cleaned up automatically; use `skill_resource_delete` beforehand if you need a clean directory too.\n\n**Make a skill available to another agent (not yourself):**\n\n- There is no tool in this family to register a skill on someone else's behalf — `skill_register`/`skill_unregister` always act on the calling agent (`agentId` is taken from the tool-call context, not a parameter). To grant a skill to a different agent, that agent has to call `skill_register` itself.\n\n**Install a skill from the ClawHub marketplace:**\n\n1. `clawhub_search` to find a candidate, or `clawhub_skill_info` if you already know the slug.\n2. Check `moderation` status in the info response before proceeding.\n3. `clawhub_install` with the `slug` (and `version` if you need a specific one, otherwise latest). This downloads the archive, re-checks moderation, runs `SkillSafetyInspectorInterface.inspect()` over the bundled markdown files and everything under `scripts/`, and only then creates the skill and its resources and registers it system-wide.\n4. `skill_register` to attach it to yourself, same as any other skill.\n5. To upgrade later, use `clawhub_update` (fails if the skill was never installed via ClawHub in the first place — use `clawhub_install` for the first install).\n\n## Naming convention\n\nSkill names are kebab-case (e.g. `skill-manager`, `code-review-checklist`), matching the one real precedent in this repo (`skills/test/SKILL.md` → `name: test-skill`) and the ClawHub slug format (`gifgrep`, `owner/skill-name`). The name also becomes the tool suffix `skill__{name}`, and this project's tool-naming convention is snake_case/lowercase — avoid PascalCase or spaces in a skill name for that reason. The directory created by `skill_create` matches `name` exactly; don't rename the directory by hand without also updating `name`, or the repository will fail to load it (directory and `name` are expected to match).\n\n## Gotchas\n\n- Creating or installing a skill does **not** make it usable by anyone — it only exists in the system-wide registry. The agent that should use it still needs to call `skill_register` itself.\n- `skill_resource_write`/`read`/`delete`/`run` reject any `relativePath` that resolves outside the skill's own directory (path traversal is blocked) — paths are always relative to the skill directory, never absolute.\n- `skill_resource_read` only works on text. If a resource is binary (image, font, template) it fails with a clear error instead of returning garbled text — that error is the signal you're reading the wrong kind of file; such a resource is meant to be opened by a script (via `skill_resource_run`), not read into your context directly.\n- Writing a resource under `scripts/` (via `skill_resource_write`, or during `clawhub_install`/`clawhub_update`) runs it through the safety inspector before it lands on disk — a blocked write fails with the inspector's reason, the file is never created.\n- `skill_delete` only removes `SKILL.md`; it is not a full uninstall. If you need to fully remove a skill's footprint, clean up resources first with `skill_resource_delete`.\n- `clawhub_install` throws if the skill name is already registered — use `clawhub_update` for skills that came from ClawHub, or `skill_update` for local edits to any already-installed skill regardless of its origin.\n"
