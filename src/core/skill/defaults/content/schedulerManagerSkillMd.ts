export const SCHEDULER_MANAGER_SKILL_MD =
    "---\nname: scheduler-manager\ndescription: Use when creating, updating, deleting, pausing/resuming, or inspecting scheduled tasks — background jobs that run an agent prompt or a workflow on a timer (once or cron), optionally deliver a result to Telegram/Web, and support heartbeat-style \"check if there's anything to do, otherwise stay silent\" patterns. Explains the ScheduledTask model, cron scheduling, the execution-session vs destination-session split, and exactly which schedule_* tool to call for each action.\n---\n\n# Scheduler Manager\n\nThis skill explains how the Scheduler system works and which `schedule_*` tools to use to create and manage scheduled tasks. A scheduled task is a background job — not a live conversation — so several things behave differently from a normal chat turn; read the whole thing before calling `schedule_create`.\n\n## What a ScheduledTask is\n\n```typescript\n{\n    id: string\n    schedule: ScheduleSpec // once | cron — when it fires\n    execution: ScheduledTaskExecution // prompt | workflow — what runs\n    destination: ScheduledTaskDestination // silent | telegram | web — where the result goes\n    nextFireAt: number // computed automatically, not something you set\n    enabled: boolean // false = paused, see schedule_pause/schedule_resume\n}\n```\n\n### `schedule` — when it fires\n\n```typescript\n{ type: 'once', at: number }                                    // unix ms timestamp, fires once then the task is deleted\n{ type: 'cron', expression: string, timezone?: string }          // 6-field cron (seconds first), recurring\n```\n\n`expression` is parsed by the `cron-parser` library with a **seconds field first**: `секунды минуты часы день-месяца месяц день-недели`. Examples:\n\n- `*/30 * * * * *` — every 30 seconds\n- `0 0 */2 * * *` — every 2 hours\n- `0 0 4 * * *` — every day at 4:00\n- `0 0 10 * * 1,3,5` — Monday/Wednesday/Friday at 10:00\n- `0 0 9 1 * *` — the 1st of every month at 9:00\n\n`timezone` is an optional IANA identifier (`Europe/Moscow`, `America/New_York`, ...). Omit it and the schedule is interpreted in the server process's local timezone — always set it explicitly if the user has a specific timezone in mind, since the server's own timezone is not something you can assume.\n\n`schedule_create`/`schedule_update` validate the expression immediately (via the same cron parser used to run it) and reject the call with a clear error if it doesn't parse — you don't find out at fire time.\n\n### `execution` — what runs\n\n```typescript\n{\n    type: 'prompt'\n    agentId: string                       // must already exist\n    prompt: string\n    skills?: string[]                     // exact skill names, forced before the prompt — see below\n    sessionMode: 'ephemeral' | 'persistent'\n    guardrailPolicy?: 'safe_skip' | 'fail'  // default safe_skip if omitted\n}\n```\n\n```typescript\n{\n    type: 'workflow'\n    workflowId: string // must already exist\n    input: unknown\n}\n```\n\n`schedule_create`/`schedule_update` reject the call immediately if `agentId`/`workflowId` doesn't exist, or if any name in `skills` isn't a real skill — you get one clear error instead of a task that silently fails every time it fires.\n\n**`skills`** are forced tool calls before the prompt runs (like the `/skill` chat command) — the agent doesn't need these skills granted in its own config, and it's not asked to decide whether to use them, they just run first every time.\n\n**`sessionMode`** — this is a cost/continuity trade-off, not a stylistic choice:\n\n- `ephemeral` (recommended default) — a brand-new, never-persisted session for every single firing. No memory between wake-ups, but zero token/disk cost accumulation — the right choice for anything frequent (a 30-second heartbeat with `persistent` would be a real problem).\n- `persistent` — one session, reused across every firing of this task (id `scheduler-{taskId}`, not something you address directly). Gives the agent memory of what it already checked/said last time — genuinely useful for a heartbeat that should say \"still nothing new\" only once, then actually stay silent. Session history is auto-compacted by the normal session optimizers, but token cost per firing is still higher than ephemeral.\n\n**`guardrailPolicy`** applies only to `prompt` executions (workflows have no run-level guardrail policy, it's a per-node graph concern) — `safe_skip` auto-allows anything with no living human to ask, `fail` makes the run fail loudly instead of silently proceeding when a decision would be needed. There is no `standard`/`skip` option here — nobody is present to answer a prompt, and full bypass isn't offered.\n\n### `destination` — where the result goes\n\n```typescript\n{ type: 'silent' }                              // result discarded, only visible via schedule_run_info\n{ type: 'telegram', chatId: number }\n{ type: 'web', sessionId: string }              // must already exist — created ahead of time through normal chat\n```\n\nCritical concept: **the destination session is not the execution session.** The agent never runs its reasoning/tool-calls inside the user's real Telegram/Web chat — it runs in its own session (per `sessionMode` above), and only the **final clean result** gets written into the destination as a plain message, exactly as if the agent had said it directly in that chat. This is what keeps a frequent heartbeat from flooding a real conversation with tool-call noise, and avoids colliding with the user's live use of that same chat.\n\nFor `prompt` executions targeting a non-`silent` destination, the agent is forced to end with one of two structured outcomes — it does not freely decide what to send:\n\n- `{ action: 'skip' }` — nothing is delivered. The normal heartbeat case (\"nothing new, staying quiet\").\n- `{ action: 'respond', message: string }` — `message` is delivered as-is.\n\nIf the run fails outright (budget exceeded, a `fail`-policy guardrail decision needed, any other error), the failure is delivered to the destination the same way an ordinary response would be — you don't need to handle errors specially when designing a task.\n\n`workflow` executions have no skip concept — the raw workflow output is always delivered to a non-`silent` destination. If you need conditional silence for a workflow task, build that as a branch in the graph itself.\n\n## The golden rule: never hand-edit `scheduled-tasks.json`\n\nAlways go through the `schedule_*` tools. Hand-editing skips `agentId`/`workflowId`/skill validation and the automatic `nextFireAt` computation entirely — a malformed or half-filled entry fails silently at the next load instead of giving you the immediate, specific error the tools give you.\n\n## Tool reference\n\n| Tool                 | Purpose                                                                                                                                                                                                       | Key parameters                                                 |\n| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- |\n| `schedule_create`    | Create a task. Computes `nextFireAt` automatically, validates `agentId`/`workflowId`/`skills` immediately, starts `enabled: true`.                                                                            | `schedule`, `execution`, `destination`                         |\n| `schedule_update`    | Replace fields on an existing task. Each of `schedule`/`execution`/`destination` fully replaces the previous value if provided — no partial merge inside them. Recomputes `nextFireAt` if `schedule` changes. | `taskId`, plus optional `schedule`, `execution`, `destination` |\n| `schedule_delete`    | Permanently delete a task, its persistent session (if it had one), and its entire run history.                                                                                                                | `taskId`                                                       |\n| `schedule_list`      | List every scheduled task with full configuration.                                                                                                                                                            | —                                                              |\n| `schedule_info`      | Full configuration of one task.                                                                                                                                                                               | `taskId`                                                       |\n| `schedule_pause`     | Stop a task from firing without deleting it. Leaves the stored `nextFireAt` untouched.                                                                                                                        | `taskId`                                                       |\n| `schedule_resume`    | Re-enable a paused task. Recomputes `nextFireAt` fresh from now — the paused period is **not** treated as downtime to catch up on.                                                                            | `taskId`                                                       |\n| `schedule_run_now`   | Force an immediate firing, ignoring `nextFireAt`. Runs in the background, returns a `runId` right away.                                                                                                       | `taskId`                                                       |\n| `schedule_list_runs` | List run history, optionally filtered by `taskId`/`status`. Does not include the full event log.                                                                                                              | `taskId?`, `status?`                                           |\n| `schedule_run_info`  | Full detail of one run — status, complete finalized event log, delivery outcome.                                                                                                                              | `runId`                                                        |\n\n## Step-by-step scenarios\n\n**Create a heartbeat task (agent periodically checks something, speaks only when there's news):**\n\n1. Make sure the target `agentId` already exists.\n2. `schedule_create` with `execution: { type: 'prompt', agentId, prompt: \"<what to check and how to decide>\", sessionMode: 'persistent' }` — persistent so it remembers what it already reported — and a real `destination` (`telegram`/`web`).\n3. The prompt should make clear to the agent that it's expected to end with a skip/respond decision implicitly (this is enforced automatically by the run — you don't need to mention the schema in the prompt itself, just describe the actual check to perform).\n4. Test immediately with `schedule_run_now` rather than waiting for the schedule — check the outcome with `schedule_run_info`.\n\n**Create a one-off reminder:**\n\n- `schedule_create` with `schedule: { type: 'once', at: <unix ms timestamp> }`, any `execution`, and the `destination` the user should see it in. It fires exactly once, then the task is gone — no need to delete it afterward.\n\n**Create a recurring workflow task:**\n\n- Make sure the `workflowId` exists first (build it via the workflow-manager tools if not). `schedule_create` with `execution: { type: 'workflow', workflowId, input }`. No `sessionMode`/`guardrailPolicy`/`skills` — those fields don't exist on this variant.\n\n**Change just the schedule, keep everything else:**\n\n- `schedule_update` with only `taskId` and `schedule` — `execution`/`destination` are left untouched when omitted.\n\n**Temporarily silence a task without losing its configuration:**\n\n- `schedule_pause`, then `schedule_resume` later. Don't use `schedule_update` with a far-future `schedule` as a substitute — that's fragile and loses the original cadence; `pause`/`resume` is the direct tool for this.\n\n**Debug why a task isn't behaving as expected:**\n\n1. `schedule_list_runs` with `taskId` to see recent runs and their status at a glance.\n2. `schedule_run_info` on the interesting `runId` for the full event log and the exact delivery outcome.\n3. A run stuck as `skipped` on a `persistent` task usually means it collided with a still-in-flight previous firing on the same session — not a bug, the session was simply busy.\n4. A run marked `interrupted` means the process crashed mid-execution — the record was created at start, never reached a final status.\n\n## Gotchas\n\n- **`nextFireAt` is not yours to set.** It's computed by the tools (`schedule_create`/`schedule_update`/`schedule_resume`) from `schedule` — there's no parameter for it on any tool.\n- **`schedule_resume` does not replay missed occurrences.** Pausing a daily task for two weeks and resuming it fires it at the _next_ natural occurrence, not once immediately as a \"catch-up\" — that catch-up behavior is reserved for process restarts, not user-initiated pauses.\n- **`destination: telegram`/`web` silently does nothing if that channel doesn't support proactive delivery**, or if the address doesn't resolve to a live session (bot removed from the chat, `sessionId` doesn't exist) — the run fails with a clear \"destination session could not be resolved\" outcome, visible via `schedule_run_info`, rather than throwing at `schedule_create` time (existence of the _channel_ isn't re-validated at creation, only at fire time).\n- **Workflow executions never skip.** If a workflow task's `destination` isn't `silent`, every single firing delivers the raw output — there's no equivalent of the prompt path's `skip` decision.\n- **`schedule_delete` is immediate and irreversible** — it also deletes the persistent session and all run history for that task in the same call, not just the task's own config entry.\n- **Frequent tasks (seconds-level cron) with `sessionMode: 'persistent'` are a real cost risk** — every firing pays for the full session history being reprocessed (session optimizers compact it, but don't eliminate the cost). Default to `ephemeral` unless the task genuinely needs memory across wake-ups.\n"
